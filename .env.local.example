# Copy this file to .env.local — NEVER commit .env.local
DATABASE_URL="postgresql://neondb_owner:npg_Vf5FP0JhHXbj@ep-bold-unit-achcssad.sa-east-1.aws.neon.tech/neondb?sslmode=require"
AUTH_SECRET="minha-senha-super-secreta-otica-2026-xyz"
NEXTAUTH_URL="http://localhost:3000"
ECOMMERCE_API_KEY="otica-ecommerce-dev-123"
# Admin credentials — single owner account (ASM-1)
ADMIN_EMAIL="admin@example.com"
# Hash with: node -e "require('bcryptjs').hash('yourpassword', 12).then(console.log)"
ADMIN_PASSWORD_HASH=""
# Base URL for QR codes on product labels (e.g. https://loja.example.com)
LABEL_QR_BASE_URL=
# Storefront (Epic 5) — base URL for BFF server-side fetches
NEXT_PUBLIC_APP_URL=http://localhost:3000
# E-mail displayed in the storefront footer
STOREFRONT_EMAIL=contato@loja.com.br
# Store WhatsApp phone for checkout link — international format without '+' (e.g. 5511999999999)
NEXT_PUBLIC_STOREFRONT_WHATSAPP_PHONE=5511999999999
