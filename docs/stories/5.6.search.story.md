# Story 5.6: Vitrine E-commerce — Busca Client-side por Nome e Marca

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["code-review", "spec-traceability", "security-review"]
```

## Story

**As a** visitante da Vitrine,
**I want** um campo de busca no header que filtra os produtos por nome ou marca em tempo real,
**so that** encontro rapidamente o que procuro sem precisar navegar pelo catálogo completo (spec.md FR-17, FR-19, Epic 5 — Headless E-commerce Vitrine).

## Acceptance Criteria

1. **`SearchBar`** — `src/features/storefront/components/search-bar.tsx` (`'use client'`):
   - Renderiza `<input type="search">` com `placeholder="Buscar produtos..."` e `aria-label="Buscar produtos"`
   - Ao digitar, chama `router.replace('/loja/produtos?q={valor}', { scroll: false })` para atualizar a URL sem recarregar a página
   - Se o campo estiver vazio, navega para `/loja/produtos` (sem `q`)
   - Sem `useSearchParams` (evita necessidade de `<Suspense>` no layout)
   - (FR-17)

2. **`ProductSearchClient`** — `src/features/storefront/components/product-search-client.tsx` (`'use client'`):
   - Recebe `products: ExportProduct[]`, `inventory: InventoryEntry[]`, `brands: string[]`, `activeBrand?: string`
   - Usa `useSearchParams()` para ler `q`; filtra `products` em memória com `useMemo`:
     - Critério: `product.name.toLowerCase().includes(q)` **ou** `product.brand.toLowerCase().includes(q)`
     - Sem `q`: retorna todos os produtos
   - Renderiza: `<BrandFilter brands={brands} activeBrand={activeBrand} />` + contador "{n} produto(s) encontrado(s)" + `<ProductGrid products={filtered} inventory={inventory} />`
   - Zero requisições adicionais ao banco — filtragem 100% em memória do browser
   - (FR-17, FR-19)

3. **Layout atualizado** — `src/app/(storefront)/loja/layout.tsx` (Server Component):
   - Adiciona `<SearchBar />` entre o logo e `<CartHeader />` no header
   - `<Suspense fallback={null}>` **não** é necessário no layout porque `SearchBar` não usa `useSearchParams`
   - Header responsivo: SearchBar com `hidden sm:block` ou `flex-1 max-w-xs` para não quebrar mobile
   - (FR-17, FR-19)

4. **Página de listagem atualizada** — `src/app/(storefront)/loja/produtos/page.tsx` (Server Component):
   - Continua recebendo `brand` via `searchParams` (server-side filter para brand)
   - Busca de brands sempre com `getProducts()` sem filtro (para listar todas as marcas)
   - Substitui `<BrandFilter>` + `<ProductGrid>` diretamente por `<Suspense fallback={<ProductGrid.Skeleton />}><ProductSearchClient products={products} inventory={inventory} brands={brands} activeBrand={brand} /></Suspense>`
   - `ProductSearchClient` lê `q` de `useSearchParams()` e filtra em memória
   - (FR-17)

5. **Testes unitários** — `src/features/storefront/__tests__/search.test.tsx`:
   - Mock: `vi.mock('next/navigation', () => ({ useSearchParams: vi.fn(), useRouter: vi.fn(), usePathname: vi.fn() }))`
   - Mock de `@/features/storefront/components/product-grid` e `brand-filter` como stubs
   - **Casos obrigatórios:**
     - `SearchBar` renderiza input com `aria-label="Buscar produtos"`
     - `SearchBar` chama `router.replace` com `/loja/produtos?q=ray+ban` ao digitar "ray ban"
     - `SearchBar` chama `router.replace` com `/loja/produtos` (sem `q`) ao limpar o campo
     - `ProductSearchClient` com query "Ray-Ban" exibe apenas produtos cujo nome contém "Ray-Ban"
     - `ProductSearchClient` com query "oakley" filtra por marca (case-insensitive)
     - `ProductSearchClient` sem query exibe todos os produtos
     - `ProductSearchClient` com query sem match passa array vazio para `ProductGrid`
   - (FR-17)

6. **Typecheck + lint** — `npx tsc --noEmit` e `npm run lint` passam com zero erros
   - (CON-7)

## 🤖 CodeRabbit Integration

**Story Type Analysis**

**Primary Type**: Frontend (`SearchBar` + `ProductSearchClient` — Client Components, hook `useSearchParams`, `useMemo`)
**Secondary Type(s)**: None
**Complexity**: Low — componentes client-side novos + pequenas edições em layout e página existente; sem nova lógica de negócio ou banco de dados

**Specialized Agent Assignment**

**Primary Agents**:
- `@dev` — implementação e pre-commit review

**Supporting Agents**:
- None required for this story

**Quality Gate Tasks**

- [ ] Pre-Commit (`@dev`): Run `wsl bash -c 'cd /mnt/c/Users/LUCAS\ ARAG\xC3\x83O/loja-oculos && ~/.local/bin/coderabbit --prompt-only -t uncommitted'` before marking story complete
- [ ] Spec traceability (`@qa`): verificar que FR-17 está rastreado para os arquivos implementados
- [ ] Pre-PR (`@devops`): Run `wsl bash -c 'cd /mnt/c/Users/LUCAS\ ARAG\xC3\x83O/loja-oculos && ~/.local/bin/coderabbit --prompt-only --base main'` before creating PR

**CodeRabbit Focus Areas**

**Primary Focus**:
- `search-bar.tsx` — sem `useSearchParams`; `router.replace` com `scroll: false`; `aria-label` presente
- `product-search-client.tsx` — `useMemo` correto; filtra por `name` e `brand`; sem chamadas a `getProducts()` ou `fetch()`
- `layout.tsx` — `SearchBar` adicionado sem `'use client'` na layout (permanece Server Component)

**Secondary Focus**:
- `produtos/page.tsx` — `<Suspense>` envolvendo `ProductSearchClient`; brands extraídas corretamente
- Testes — mock de `next/navigation` correto; `router.replace` verificado nos casos de SearchBar

**Self-Healing Configuration**

**Expected Self-Healing**:
- Primary Agent: `@dev` (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL only

**Predicted Behavior**:
- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only (noted in Dev Notes)

## Dev Notes

### Previous Story Insights (Story 5.5)

- `layout.tsx` permanece Server Component — `SearchBar` é uma client island inserida nele
- `getProducts()` e `getInventory()` em `src/lib/storefront/api.ts` — imports diretos do `ExportService`
- `<ProductGrid>` e `<BrandFilter>` já existem — reutilizar sem modificação
- `BrandFilter` recebe `brands: string[]` e `activeBrand?: string` — manter assinatura
- `ExportProduct` e `InventoryEntry` exportados de `@/lib/storefront/api`

### SearchBar — Estrutura Sugerida

```tsx
// src/features/storefront/components/search-bar.tsx
'use client'

import { useRouter } from 'next/navigation'
import { useCallback } from 'react'

export function SearchBar() {
  const router = useRouter()

  const handleChange = useCallback(
    (e: React.ChangeEvent<HTMLInputElement>) => {
      const val = e.target.value.trim()
      if (val) {
        router.replace(`/loja/produtos?q=${encodeURIComponent(val)}`, { scroll: false })
      } else {
        router.replace('/loja/produtos', { scroll: false })
      }
    },
    [router],
  )

  return (
    <input
      type="search"
      placeholder="Buscar produtos..."
      aria-label="Buscar produtos"
      onChange={handleChange}
      className="w-full max-w-xs rounded-md border border-gray-300 px-3 py-1.5 text-sm focus:border-gray-500 focus:outline-none"
    />
  )
}
```

> **Sem `useSearchParams`**: o input é uncontrolled — não reflete o valor atual da URL ao navegar diretamente para `/loja/produtos?q=...` via link externo. Aceitável para MVP; campo simplesmente fica vazio nesses casos.

### ProductSearchClient — Estrutura Sugerida

```tsx
// src/features/storefront/components/product-search-client.tsx
'use client'

import { useMemo } from 'react'
import { useSearchParams } from 'next/navigation'
import type { ExportProduct, InventoryEntry } from '@/lib/storefront/api'
import { ProductGrid } from './product-grid'
import { BrandFilter } from './brand-filter'

interface ProductSearchClientProps {
  products: ExportProduct[]
  inventory: InventoryEntry[]
  brands: string[]
  activeBrand?: string
}

export function ProductSearchClient({
  products,
  inventory,
  brands,
  activeBrand,
}: ProductSearchClientProps) {
  const searchParams = useSearchParams()
  const q = (searchParams.get('q') ?? '').toLowerCase().trim()

  const filtered = useMemo(() => {
    if (!q) return products
    return products.filter(
      (p) =>
        p.name.toLowerCase().includes(q) ||
        p.brand.toLowerCase().includes(q),
    )
  }, [products, q])

  return (
    <>
      <BrandFilter brands={brands} activeBrand={activeBrand} />
      <p className="mt-2 text-sm text-gray-500">{filtered.length} produto(s) encontrado(s)</p>
      <div className="mt-6">
        <ProductGrid products={filtered} inventory={inventory} />
      </div>
    </>
  )
}
```

### Página de Listagem Atualizada — Estrutura

```tsx
// src/app/(storefront)/loja/produtos/page.tsx
import { Suspense } from 'react'
import { getProducts, getInventory } from '@/lib/storefront/api'
import { ProductSearchClient } from '@/features/storefront/components/product-search-client'
import { ProductGrid } from '@/features/storefront/components/product-grid'

interface ListingPageProps {
  searchParams: Promise<{ brand?: string }>
}

export default async function ProductListingPage({ searchParams }: ListingPageProps) {
  const { brand } = await searchParams
  const [products, allProducts, inventory] = await Promise.all([
    getProducts(brand),
    getProducts(),
    getInventory(),
  ])
  const brands = [...new Set(allProducts.map((p) => p.brand))].sort()

  return (
    <div className="mx-auto max-w-7xl px-4 py-10">
      <div className="mb-6">
        <h1 className="text-2xl font-bold text-gray-900">Produtos</h1>
      </div>
      <Suspense fallback={<ProductGrid.Skeleton />}>
        <ProductSearchClient
          products={products}
          inventory={inventory}
          brands={brands}
          activeBrand={brand}
        />
      </Suspense>
    </div>
  )
}
```

### Layout Atualizado — Estrutura

```tsx
// src/app/(storefront)/loja/layout.tsx
import Link from 'next/link'
import { CartHeader } from '@/features/storefront/components/cart-header'
import { SearchBar } from '@/features/storefront/components/search-bar'

export default function StorefrontLayout({ children }: { children: React.ReactNode }) {
  const email = process.env.STOREFRONT_EMAIL ?? 'contato@loja.com.br'
  const year = new Date().getFullYear()

  return (
    <div className="flex min-h-screen flex-col">
      <header className="border-b border-gray-200 bg-white">
        <div className="mx-auto flex max-w-7xl items-center gap-4 px-4 py-4">
          <Link href="/loja" className="text-lg font-bold text-gray-900 shrink-0">
            Ótica da Loja
          </Link>
          <div className="hidden flex-1 sm:flex">
            <SearchBar />
          </div>
          <CartHeader />
        </div>
      </header>

      <main className="flex-1">{children}</main>

      <footer className="border-t border-gray-200 bg-gray-50">
        <div className="mx-auto flex max-w-7xl flex-col items-center gap-1 px-4 py-6 text-center text-sm text-gray-500 sm:flex-row sm:justify-between">
          <span>© {year} Ótica da Loja. Todos os direitos reservados.</span>
          <span>
            Contato:{' '}
            <a href={`mailto:${email}`} className="hover:text-gray-700">
              {email}
            </a>
          </span>
        </div>
      </footer>
    </div>
  )
}
```

### Testes — Abordagem

`SearchBar` e `ProductSearchClient` são Client Components — testáveis diretamente com RTL + Vitest.

```typescript
// Mocks de next/navigation
const replaceMock = vi.fn()
vi.mock('next/navigation', () => ({
  useRouter: () => ({ replace: replaceMock }),
  useSearchParams: vi.fn().mockReturnValue(new URLSearchParams()),
  usePathname: vi.fn().mockReturnValue('/loja/produtos'),
}))

// Mock de ProductGrid e BrandFilter
vi.mock('@/features/storefront/components/product-grid', () => ({
  ProductGrid: ({ products }: { products: unknown[] }) => (
    <div data-testid="product-grid" data-count={products.length} />
  ),
}))

vi.mock('@/features/storefront/components/brand-filter', () => ({
  BrandFilter: () => <div data-testid="brand-filter" />,
}))
```

Para testar o filtro por `q`:
```typescript
// Alterar useSearchParams mock para retornar q=ray-ban
vi.mocked(useSearchParams).mockReturnValue(new URLSearchParams('q=ray-ban'))
```

### Variáveis de Ambiente

Nenhuma variável nova. Esta story usa apenas componentes client-side e dados já carregados.

### Dependências Disponíveis

Todas em `package.json` — sem `npm install`:
- `next/navigation` — `useSearchParams`, `useRouter`
- `react` — `useMemo`, `useCallback`
- `tailwindcss` — estilização do input
- `@testing-library/react ^16.3.2` + `vitest ^3.2.1` — testes RTL

### Fluxo de Dados (sem requisições adicionais)

```
Usuário digita "ray ban"
  → SearchBar.onChange
  → router.replace('/loja/produtos?q=ray+ban', { scroll: false })
  → Next.js soft navigation (RSC cache hit — server não re-executa getProducts)
  → ProductSearchClient re-render (useSearchParams() lê q='ray ban')
  → useMemo filtra products[] em memória
  → ProductGrid exibe produtos filtrados
  ✓ Zero requisições HTTP adicionais
```

## Tasks / Subtasks

- [x] 1. Criar `src/features/storefront/components/search-bar.tsx` — Client Component com `<input type="search">`, `aria-label`, `router.replace` sem `useSearchParams` (AC: 1)
- [x] 2. Criar `src/features/storefront/components/product-search-client.tsx` — Client Component com `useSearchParams`, `useMemo`, filtragem por name+brand, renderiza `<BrandFilter>` + contador + `<ProductGrid>` (AC: 2)
- [x] 3. Atualizar `src/app/(storefront)/loja/layout.tsx` — adicionar `<SearchBar />` no header entre logo e `<CartHeader />` com `hidden sm:flex` wrapper (AC: 3)
- [x] 4. Atualizar `src/app/(storefront)/loja/produtos/page.tsx` — substituir `<BrandFilter>` + `<ProductGrid>` direto por `<Suspense><ProductSearchClient .../></Suspense>` (AC: 4)
- [x] 5. Escrever testes — `src/features/storefront/__tests__/search.test.tsx`: 9 casos cobrindo SearchBar render, router.replace com q, router.replace sem q, filtro por nome, filtro por marca, sem query = todos, sem match = array vazio, contagem, BrandFilter (AC: 5)
- [x] 6. Rodar `npx tsc --noEmit` + `npm run lint` — zero erros (AC: 6)
- [ ] 7. Teste visual: iniciar dev server, acessar `/loja/produtos`, digitar "ray" no campo de busca, confirmar filtro em tempo real sem reload de página

## File List

**New files:**
- `src/features/storefront/components/search-bar.tsx` — campo de busca no header (Client Component)
- `src/features/storefront/components/product-search-client.tsx` — grid filtrável client-side
- `src/features/storefront/__tests__/search.test.tsx` — testes RTL da busca

**Modified files:**
- `src/app/(storefront)/loja/layout.tsx` — adiciona `<SearchBar />` ao header
- `src/app/(storefront)/loja/produtos/page.tsx` — usa `<ProductSearchClient>` com `<Suspense>`

## QA Results

_Pendente — story em Draft_

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-16 | 1.0 | Story draft criado — Epic 5 Busca Client-side (FR-17) | @sm (River) |
| 2026-05-16 | 1.1 | Validated GO (10/10) — Status: Draft → Ready | @po (Pax) |
| 2026-05-16 | 1.2 | Development complete — 9 RTL tests passing, tsc/lint clean — Status: Ready → InReview | @dev (Dex) |
